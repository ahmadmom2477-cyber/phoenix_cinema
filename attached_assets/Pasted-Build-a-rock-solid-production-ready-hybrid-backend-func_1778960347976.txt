Build a rock-solid, production-ready hybrid backend function for the `/api/torrent-info/:imdbId` endpoint. This endpoint must perfectly handle both movies and TV shows to eliminate all 404 errors and network timeouts. Follow these strict technical requirements:

### 1. Hybrid Request Routing (Conditional Routing)
- Upon receiving the `imdbId`, the backend must first fetch from the official YTS API mirror: `https://yts.pm/api/v2/list_movies.json?query_term=${imdbId}`. (If it timeouts or fails, immediately fallback to trying `https://yts.cx/api/v2/list_movies.json?query_term=${imdbId}`).
- **If results are found (movie_count > 0):** Treat it strictly as a [Movie].
- **If NO results are found:** Automatically fallback and fetch from the Apibay API for TV shows/packs: `https://apibay.org/q.php?q=${imdbId}`. Treat it strictly as a [TV Show].

### 2. Movie Processing Engine (WebSeed Generation)
- Filter out all 4K and 2160p qualities to prevent client-side browser memory crashes. Prioritize 1080p, then 720p.
- Extract the torrent `hash` and build a standard high-speed WebSeed URL using this exact pattern: `https://fr.yts.mx/torrent/download/${bestTorrent.hash}`.
- Construct a full hybrid magnet link that embeds this WebSeed via the `&ws=` parameter.
- **Crucial Response:** Return a JSON containing the structured info: `{ type: "movie", title: movie.title, hash: bestTorrent.hash, quality: bestTorrent.quality, size: bestTorrent.size, webSeedUrl: webSeedUrl, magnet: fullMagnetWithWebSeed }`.

### 3. TV Show Processing Engine (Tracker Injection)
- If the request falls back to Apibay, sort the returned torrent array by the highest number of `seeders` to ensure active connectivity.
- Inject the following high-performance Tier-1 and WebRTC trackers directly into the generated magnet link string using `&tr=` parameters:
  - `udp://tracker.coppersurfer.tk:6969/announce`
  - `udp://tracker.leechers-paradise.org:6969/announce`
  - `udp://open.demonii.com:1337/announce`
  - `udp://tracker.opentrackr.org:13337/announce`
  - `wss://tracker.openwebtorrent.com` (Crucial for Client-side browser WebRTC)
  - `wss://tracker.btorrent.xyz` (Crucial for Client-side browser WebRTC)
- **Crucial Response:** Return a JSON containing: `{ type: "show", name: bestShow.name, hash: bestShow.info_hash, seeders: bestShow.seeders, magnet: fullyLoadedMagnet }`.

### 4. Client-Side (Frontend) Player Binding
- Ensure the frontend streaming player (using WebTorrent/video player components) receives this JSON response.
- Update the frontend logic: If `webSeedUrl` is present in the response, instruct the WebTorrent client to fetch pieces via HTTP WebSeed instead of regular P2P peers. This ensures instantaneous streaming and bypasses local ISP P2P/UDP blocking.
- Never throw a blind 404 or let the response hang. Always return a proper fallback response or explicit descriptive errors.

Rewrite the backend route file (e.g., `torrent-info.ts` or its equivalent) and test the integration now.
