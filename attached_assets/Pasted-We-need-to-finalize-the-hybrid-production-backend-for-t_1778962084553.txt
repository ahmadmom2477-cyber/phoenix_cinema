We need to finalize the hybrid production backend for the `/api/torrent-info/:imdbId` endpoint and connect it flawlessly to the frontend player (`TorrentPlayer.tsx`). The primary goal is to fetch high-quality streaming assets while bypass-coding around Cloudflare's blocks on YTS mirrors.

Please implement the backend and frontend systems using the exact architecture below:

### 1. Robust Architecture & Proxy Logic (Backend: torrent-file.ts)
- Do NOT make direct requests to standard YTS mirrors (like yts.mx or yts.pm) to avoid the Cloudflare HTML challenge.
- **For Movies:** Fetch the YTS torrent metadata and hash using the unblocked public developer proxy endpoint: `https://yts-proxy.now.sh/api/v2/list_movies.json?query_term=${imdbId}`.
- If the proxy returns results (`movie_count > 0`), filter out 4K/2160p qualities. Take the 1080p or 720p hash and programmatically construct the high-speed HTTP WebSeed download string: `https://fr.yts.mx/torrent/download/${hash.toLowerCase()}`.
- **For TV Shows (Fallback):** If the movie count is 0, query the EZTV / Apibay API for show packages: `https://apibay.org/q.php?q=${imdbId}`. Sort results by highest seeders.

### 2. Tracker Injection & Magnet Assembly
- For both Movies and TV Shows, assemble a clean magnet link dynamically.
- Inject the following 5 bulletproof WebRTC secure trackers to ensure the browser player can build instant client-side connections:
  - `wss://tracker.openwebtorrent.com`
  - `wss://tracker.btorrent.xyz`
  - `wss://tracker.files.fm:7073/announce`
  - `wss://peertube2.cpy.re:443/tracker/socket`
  - `wss://tracker.gbitt.info:443/announce`
- Return a structured JSON response to the client:
  - For Movies: `{ type: "movie", title: movie.title, magnet: finalMagnetWithWebSeed, webSeedUrl: constructedWebSeedUrl }`
  - For Shows: `{ type: "show", title: bestShow.name, magnet: finalMagnetWithTrackers }`

### 3. Frontend Binding (Frontend: TorrentPlayer.tsx)
- Catch the JSON payload from the backend endpoint.
- If `webSeedUrl` is present in the response, pass it directly into the WebTorrent client instance via the `urlList` fallback options array. This ensures instantaneous chunk streaming inside the UAE via HTTP protocol, completely bypassing ISP P2P throttling.
- If it's a TV show, initialize WebTorrent to rely on the injected WebRTC `wss://` trackers seamlessly.

Apply this codebase logic, eliminate all old static filtering, and verify the builds are ready for GitHub and Render deployment.
